<p><%= t("devise.mailer.reset_password.greeting", email: @resource.email) %></p>

<p><%= t("devise.mailer.reset_password.request_info") %></p>

<p><%= link_to t("devise.mailer.reset_password.change_link"), edit_password_url(@resource, reset_password_token: @token, locale: I18n.locale) %></p>

<p><%= t("devise.mailer.reset_password.ignore_note") %></p>
<p><%= t("devise.mailer.reset_password.reminder") %></p>
