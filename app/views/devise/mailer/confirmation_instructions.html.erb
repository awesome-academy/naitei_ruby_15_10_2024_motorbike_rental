<p><%= t("devise.mailer.confirmation_email.greeting", email: @email) %></p>

<p><%= t("devise.mailer.confirmation_email.instruction") %></p>

<p><%= link_to t("devise.mailer.confirmation_email.confirm_link"), confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale) %></p>
